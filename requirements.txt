# Server-side requirements (for Linux server with RTX 5090)

# Core ML/AI
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.20.0
qwen-vl-utils>=0.0.2

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0

# Utilities
numpy>=1.24.0

# Optional: for better performance
# bitsandbytes>=0.41.0  # For quantization
# flash-attn>=2.0.0  # For faster attention (requires CUDA)
